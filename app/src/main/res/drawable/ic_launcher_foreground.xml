<?xml version="1.0" encoding="utf-8"?>
<!-- Small additional padding keeps the artwork inside every adaptive-icon mask. -->
<inset xmlns:android="http://schemas.android.com/apk/res/android" android:inset="8%">
    <bitmap
        android:src="@drawable/nocap_foreground"
        android:gravity="fill"
        android:filter="true" />
</inset>
